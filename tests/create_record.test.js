describe("Stock Data", () => {
  describe("Initialization", () => {
    it("should create a base chart record with the date, and adjusted close price", () => {});
    it("should throw an error if there are not enough arguments", () => {});
    it("should throw an error if the arguments aren't the proper type", () => {});
  });

  //   describe("Update Record With Bollinger Band Data", () => {
  //     it("the base record should include the date, and adjusted close price", () => {});
  //     it("should update a base chart record with the upper and lower bollinger bands, the simple moving average, whether the daily adjusted close was above or below the bollinger bands, and the band width", () => {});
  //     it("should throw an error if there are not enough arguments", () => {});
  //     it("should throw an error if the arguments aren't the proper type", () => {});
  //   });

  //   describe("Update Record With Accumulation and Distribution Data", () => {
  //     it("the base record should include the date, adjusted close price, upper and lower bollinger bands, the simple moving average, whether the daily adjusted close was above or below the bollinger bands, and the band width", () => {});
  //     it("should update a base chart record with the accumulation and distribution percentage", () => {});
  //     it("should throw an error if there are not enough arguments", () => {});
  //     it("should throw an error if the arguments aren't the proper type", () => {});
  //   });

  //   describe("Update Record With Relative Strength Index Data", () => {
  //     it("the base record should include the date, adjusted close price, upper and lower bollinger bands, the simple moving average, whether the daily adjusted close was above or below the bollinger bands, accumulation and distribution percentage, and the band width", () => {});
  //     it("should update a base chart record with the relative strength index, whether the stock is over bought or sold, and the current relative strength index trend", () => {});
  //     it("should throw an error if there are not enough arguments", () => {});
  //     it("should throw an error if the arguments aren't the proper type", () => {});
  //   });

  //   describe("Final Data Checker", () => {
  //     it("should return true if the final data matches the expected form", () => {});
  //     it("should return false if the final data doesn't match the expected form", () => {});
  //   });
});
